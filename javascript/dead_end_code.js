let fizzBuzz = (length) => {
  array = []
  array.push(length)
  return array
}

// -----------------------------------------------------------------

let fizzBuzz = (length) => {
  var array = []
   if (length === 3) {
      var x = ["fizz"];
 } else if (length === 5) {
      var y = ["buzz"];
 } else
   var z = array.push
}

// -----------------------------------------------------------------

let fizzBuzz = (N) => {
  array = Array.apply(null, {length: N}).map(Number.call, Number)
  return array
}

// -----------------------------------------------------------------

let fizzBuzz = (number) => {
    var numArray = [];
    for(var i=0; i<number; i++){
        numArray.push(i);
    }
    var a = numArray
    var b = numArray.shift()
    return a
  }

export {fizzBuzz}

// -----------------------------------------------------------------
let fizzBuzz = (number) => {
    var numArray = [];
    for(var i=0; i<number; i++){
        numArray.push(i);
    }
    var item =
    a = numArray.map(function(item) { return item % 3 && 5 === 0 ? "fizzBuzz" : item; });
    b = a.map(function(item) { return item % 3 === 0 ? "fizz" : item; });
    c = b.map(function(item) { return item % 5 === 0 ? "buzz" : item; });
    return c
  }
// -----------------------------------------------------------------
  let fizzBuzz = (number) => {
    var numArray = [];
      for (i=1; i<=number; i++) {
        if ( i % 3 === 0 && i % 5 === 0) {
          numArray.push("FizzBuzz"); }
        else if ( i % 3 === 0) {
          numArray.push("Fizz"); }
        else if ( i % 5 === 0) {
            numArray.push("Buzz"); }
        else {
          numArray.push(i);
        }
      }
    }
